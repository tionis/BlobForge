{
  "name": "blobforge-bunny-coordinator",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=esm --platform=browser --target=es2022 --outfile=dist/index.js --external:@bunny.net/edgescript-sdk",
    "check": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@bunny.net/edgescript-sdk": "^0.11.2",
    "@libsql/client": "^0.15.15"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "esbuild": "^0.25.3",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
