[project]
name = "blobforge"
version = "0.4.0"
description = "Media conversion orchestration with local storage and reproducible artifacts"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
authors = [
    { name = "BlobForge Contributors" }
]
keywords = ["media", "pdf", "conversion", "distributed", "markdown", "mdaf"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Text Processing :: Markup :: Markdown",
]

dependencies = [
    "blake3>=1.0.9",
    "boto3>=1.28.0",
    "jsonschema>=4.26.0",
]

[project.optional-dependencies]
convert = [
    "marker-pdf>=1.10.2,<2",
]
metrics = [
    "psutil>=5.9.0",
]
xattr = [
    "xattr>=1.0.0",
]
dev = [
    "pytest>=7.0.0",
    "psutil>=5.9.0",
    "xattr>=1.0.0",
    "httpx>=0.28.1",
]
all = [
    "marker-pdf>=1.10.2,<2",
    "psutil>=5.9.0",
    "xattr>=1.0.0",
    "fastapi>=0.141.1",
    "uvicorn>=0.52.4",
    "authlib>=1.6.5",
    "httpx>=0.28.1",
    "itsdangerous>=2.2.0",
]
server = [
    "authlib>=1.6.5",
    "fastapi>=0.141.1",
    "httpx>=0.28.1",
    "itsdangerous>=2.2.0",
    "uvicorn>=0.52.4",
]

[project.scripts]
blobforge = "blobforge.cli:main"

[project.urls]
Repository = "https://github.com/tionis/BlobForge"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["blobforge"]

# Keep the universal lockfile above the patched version for every supported
# Python/platform resolution. Pillow is an override because marker-pdf 1.10.2
# still caps it below the first patched release.
[tool.uv]
constraint-dependencies = [
    "cryptography>=48.0.1",
    "filelock>=3.20.3",
    "idna>=3.15",
    "pyasn1>=0.6.3",
    "pydantic-settings>=2.14.2",
    "pygments>=2.20.0",
    "pytest>=9.0.3",
    "python-dotenv>=1.2.2",
    "requests>=2.33.0",
    "scikit-learn>=1.5.0",
    "soupsieve>=2.8.4",
    # 2.10 fixes the reachable sequence APIs while retaining CUDA 12 worker
    # compatibility; the remaining jit.script advisory is not used here.
    "torch==2.10.0",
    "urllib3>=2.7.0",
]
override-dependencies = [
    "pillow>=12.2.0",
]
