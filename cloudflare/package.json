{
  "name": "blobforge-coordinator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.71",
    "@cloudflare/workers-types": "4.20250917.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4",
    "wrangler": "4.35.0"
  }
}
